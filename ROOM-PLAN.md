# ROOM-PLAN.md — ห้องแข่งออนไลน์ ("สร้างห้อง → ใส่รหัส → แข่งกัน")

เขียน 2026-07-31 · อ้างอิงสแตกจริงใน `DEPLOY.md` + `ecosystem.config.cjs`

## สถานะ (อัปเดต 2026-07-31)

| ขั้น | สถานะ |
|---|---|
| 0 · `Cache-Control: no-store` | ✅ ทำแล้ว (`server/lib.php`) |
| 0 · ย้าย `bg7-api` ไป IIS+FastCGI | ⛔ **ยังไม่ทำ — เป็นงานบนเครื่องเซิร์ฟเวอร์ ต้องรันเอง** (ภาคผนวก C ของ `DEPLOY.md`) · จนกว่าจะย้าย ให้จำกัดห้องละ ≤ 4 ทีม และห้ามอัปโหลดวิดีโอระหว่างแข่ง |
| 1 · ตาราง + `server/room.php` | ✅ ทำแล้ว + ทดสอบด้วย `curl` ครบทุก action |
| 2 · `roomApi.ts` + หน้า สร้าง/เข้าร่วม/ล็อบบี้ + ล็อกกติกา | ✅ ทำแล้ว (`src/core/roomApi.ts`, `src/screens/Room/RoomPanel.tsx`) |
| 3 · heartbeat + แผงอันดับในแถบขวา | ✅ ทำแล้ว (`src/core/useRoomHeartbeat.ts`, `src/components/RoomStandings.tsx`) |
| 4 · หมดเวลา/ถึงเป้า → จอสรุปอันดับ | ✅ ทำแล้ว (`RoomResult` ใน `GameOver.tsx`) |
| 5 · ทดสอบกับอุปกรณ์จริง 2 เครื่อง | ⛔ ยังไม่ได้ทดสอบบนอุปกรณ์จริง |
| 6 · การ์ดป่วนข้ามทีม | ✅ ทำแล้ว + ทดสอบกฎด้วย `curl` (อันดับ/คูลดาวน์/คิวเต็ม/ส่งมอบครั้งเดียว) |

**สิ่งที่เปลี่ยนจากแผนตอนออกแบบ (ตั้งใจ):**
- เวลาที่ใช้ตัดสินเกมเก็บเป็น **unix timestamp (INT)** ไม่ใช่ `DATETIME` — กันปัญหา timezone ของ PHP กับ MySQL ไม่ตรงกัน ซึ่งจะทำให้เวลาแข่งเพี้ยนข้ามเครื่อง
- เพิ่มคอลัมน์ `suspect` — ธงเตือนแต้มพุ่งผิดปกติ (ไม่ปฏิเสธ)
- **ในโหมดห้อง เกมในเครื่องไม่จบเอง** — server เป็นคนประกาศจบ (หมดเวลา/มีทีมถึงเป้า) เพราะคะแนนทีมคือผลรวมของทุกคนในเครื่อง ซึ่งถึงเป้าก่อนที่ผู้เล่นคนใดคนหนึ่งจะถึง ถ้าปล่อยให้เช็กแบบเดิมด้วย เครื่องจะจบไม่พร้อมห้อง = อันดับเพี้ยน
- ผู้เล่นในโหมดห้องใช้ **หมากกษัตริย์/ชื่อค่าเริ่มต้น** (ไม่ได้เลือกเองในล็อบบี้) — ตัดออกเพื่อให้ v1 เล็กลง

---

---

## 1) รูปแบบที่เลือก: **แข่งขนาน (parallel race)**

แต่ละทีมเล่น **กระดานของตัวเอง** พร้อมกัน แล้วเห็นความคืบหน้าของทีมอื่นสด ๆ บนจอ

```
เจ้าของห้อง                          ผู้เล่นอื่น
─────────────────                    ──────────
"สร้างห้องแข่ง" → ตั้งกติกา
  30 นาที · 7 เหรียญ · ระดับกลาง · 2 คน/เครื่อง
  ได้รหัส  7MHR42  + QR บนจอ
                              →  ใส่รหัส / สแกน QR → เข้าล็อบบี้
  เห็นทีมที่เข้ามาแล้ว
  "เริ่มแข่ง"                  →  ทุกเครื่องนับถอยหลัง 3-2-1 พร้อมกัน

  ระหว่างแข่ง: กระดานของใครของมัน
  แถบขวามีแผงอันดับสด  🥇 ทีมข 4 · 🥈 เรา 3 · 🥉 ทีมค 2   (อัปเดตทุก 3 วิ)

  หมดเวลา หรือมีทีมถึงเป้า → จอสรุปอันดับของทุกทีม
```

**ทำไมแบบนี้:** `store.ts` ยังเป็นเจ้าของกติกาเหมือนเดิม **ไม่ต้องเขียนใหม่** ห้องแข่งเป็นแค่ "ช่องทางข้าง" ที่รายงานเหรียญออกไปและรับอันดับกลับมาแสดง — โครงสร้างเดียวกับ `challenge.php` ที่พิสูจน์แล้วว่าทำงานได้บนไวไฟโรงเรียน

**ไม่เลือกแบบกระดานเดียวกัน (shared board)** เพราะต้องย้ายกติกาทั้งหมดขึ้น server = เขียนใหม่ 2-3 เดือน และผู้เล่นอีกฝั่งต้องนั่งดูจอเฉย ๆ 3 ใน 4 ของเวลา (เกมผลัดตา) · ของที่ทำในแผนนี้ใช้ต่อได้ทั้งหมดถ้าวันหน้าจะไปทางนั้น

---

## 2) ⚠️ ข้อจำกัดโฮสต์ที่ต้องจัดการ **ก่อน** เปิดให้ผู้เล่นอื่นเข้า

### ① `php -S` บน Windows รับได้ทีละ 1 คำขอ

`ecosystem.config.cjs` รัน `php -S 127.0.0.1:8000` ซึ่งเป็น **single-threaded** — `PHP_CLI_SERVER_WORKERS` ใช้ `fork()` จึงมีเฉพาะบน Unix **ไม่มีผลบน Windows** → คำขอที่ 2 ต้องรอคำขอแรกเสร็จเสมอ

งบคำขอต่อ 1 ทีม (ประมาณการ):

| ตัวที่ยิง | ความถี่ | req/s |
|---|---|---|
| แท็บเล็ต poll สถานะห้อง | ทุก 3 วิ | 0.33 |
| แท็บเล็ต poll ผลคำถาม (`challenge.php`) | ทุก 1.3 วิ (เฉพาะตอนการ์ดเปิด) | 0.77 |
| มือถือ poll ว่าข้อปิดยัง | ทุก 2 วิ (เฉพาะตอนตอบ) | 0.5 |
| **รวมช่วงพีก** | | **~1.6** |

- 4 ทีม ≈ 6.4 req/s → **ไหว**
- 10 ทีม ≈ 16 req/s → เสี่ยง (คำขอละ 30ms = เพดานทฤษฎี ~33 req/s ไม่มี headroom)

**ทางเลือก**
- **แนะนำ:** ย้าย `bg7-api` ไป **IIS + PHP FastCGI** ตาม **ภาคผนวก C ของ `DEPLOY.md`** (เขียนไว้แล้ว) — เป็นงาน config ล้วน **ไม่ต้องแก้โค้ดสักบรรทัด** แล้ว Cloudflare ชี้ไปพอร์ต IIS แทน
- **ถ้ายังไม่ย้าย:** จำกัดห้องละ **≤ 4 ทีม** และยืดการ poll สถานะห้องเป็น 4-5 วิ

### ② อัปโหลดวิดีโอ AR จะแช่แข็งทั้งเซิร์ฟเวอร์

`upload.php` รับไฟล์ได้ถึง 200 MB · ระหว่างที่ PHP รับไฟล์อยู่ **คำขออื่นทุกตัวถูกบล็อกหมด** (จากข้อ ①) → ทุกห้องที่กำลังแข่งจะค้างพร้อมกัน

**กติกาที่ต้องมี:** ห้ามอัปโหลดวิดีโอ/นำเข้า Excel ระหว่างมีห้องที่สถานะ `running` · ทางที่ดีกว่าคือย้ายไป IIS ตามข้อ ① แล้วปัญหานี้หายไปเอง

### ③ Cloudflare อาจแคชคำตอบของ poll

`send_json()` ใน `server/lib.php` **ไม่ได้ส่ง `Cache-Control`** เลย · วันนี้ไม่มีปัญหาเพราะทุก poll มี `?id=` ไม่ซ้ำ แต่ห้องแข่งจะยิง URL **เดิมซ้ำ ๆ** (`room.php?code=7MHR42`) ถ้าโซนนี้มี Cache Rule แบบ "Cache Everything" (พบบ่อยในองค์กร) อันดับจะค้างไม่อัปเดต แล้วดีบักยากมาก

**ต้องทำ:** เพิ่ม `Cache-Control: no-store` ใน `send_json()` (แก้ที่เดียว ได้ผลทุก endpoint)

### ④ สิ่งที่ **ไม่ต้อง** เปลี่ยน
- **ไม่ต้องเพิ่ม subdomain / ไม่ต้องแตะ Cloudflare dashboard** — `room.php` อยู่ใต้ `/api` เดิม เป็น same-origin ผ่าน proxy ของ vite อยู่แล้ว
- **ไม่ต้องมี WebSocket** — polling พอสำหรับเกมผลัดตา และเข้ากับ tunnel/โฮสต์ปัจจุบัน
- **ห้ามใช้ long-polling / SSE เด็ดขาด** ตราบใดที่ยังใช้ `php -S` — การค้างคอนเนกชันไว้ = บล็อกทั้งเซิร์ฟเวอร์

---

## 3) สคีมาฐานข้อมูล (เพิ่ม 2 ตาราง)

ของจริงอยู่ใน `server/schema.sql` แล้ว (และ `room.php` มี `ensure_room_tables()` สร้างให้อัตโนมัติ
สำหรับ DB เดิม → **redeploy ไม่ต้องรัน migration เอง**)

- `room` — code / host_name / status / rules(JSON) / started_at / ends_at / created_at
- `room_team` — room_code / team_token / team_name / king_coins / coins / finished_at / suspect / last_seen

⚠️ `started_at` `ends_at` `finished_at` `last_seen` เป็น **INT (unix timestamp) ที่คิดด้วย `time()` ของ PHP ฝั่งเดียว**
ห้ามเปลี่ยนไปใช้ `NOW()` ของ MySQL เพราะ timezone ของสองตัวอาจไม่ตรงกัน แล้วเวลาแข่งจะเพี้ยน
(`created_at` เป็น TIMESTAMP ได้ เพราะใช้เก็บกวาดห้องเก่าอย่างเดียว)

ล้างห้องเก่า (เกิน 6 ชม.) **เฉพาะตอน `create` เท่านั้น** — ห้ามใส่ไว้หัวไฟล์ ไม่งั้น `sync` ทุก 3 วิ/ทีม
จะลาก `DELETE + subquery` ไปด้วยทุกครั้งบนเซิร์ฟเวอร์ที่รับทีละคำขอ

---

## 4) API — ไฟล์เดียว `server/room.php`

ตามแบบ `challenge.php` (action อยู่ใน body) · helper ใช้ `lib.php` ร่วม

| action | auth | ทำอะไร |
|---|---|---|
| `POST {action:'create', rules}` | **Bearer token ครู** | สร้างห้อง คืน `code` |
| `POST {action:'join', code, teamName}` | ไม่ต้อง | เข้าห้อง คืน `teamToken` + `rules` + `contentVersion` |
| `POST {action:'start', code}` | **Bearer token ครู** | ตั้ง `started_at`/`ends_at` จาก **เวลา server** |
| `POST {action:'sync', code, teamToken, kingCoins, coins}` | teamToken | **หัวใจของระบบ:** รายงานแต้มของตัวเอง **แล้วคืนสถานะห้องทั้งหมดในคำตอบเดียว** |
| `GET ?code=XXXX` | ไม่ต้อง | อ่านสถานะห้อง (ล็อบบี้/จอฉาย) |

**`sync` ต้องเป็น round trip เดียว** (ส่งแต้ม + รับอันดับ) ไม่ใช่ 2 endpoint — บนเซิร์ฟเวอร์ที่รับทีละคำขอ การประหยัด round trip คือการประหยัดคอขวดโดยตรง

**เข้าร่วมไม่ต้องมีบัญชี** (เหมือน PIN ของ Kahoot) → **v1 จึงยังไม่ต้องทำ multi-tenant / บัญชีครูแยกรายกลุ่ม** เพราะผู้เล่นอื่นแค่ "เข้าร่วม" ไม่ได้เข้า CMS · ถ้าวันหน้าอยากให้ผู้เล่นอื่นแก้การ์ดเองค่อยทำเรื่อง tenancy (นั่นเป็นอีกโปรเจกต์)

---

## 5) ฝั่งเว็บ

**ไฟล์ใหม่**
- `src/core/roomApi.ts` — client เรียก `/api/room.php` · **standalone ห้าม import store** (แบบเดียวกับ `challengeApi.ts`)
- `src/screens/Room/CreateRoom.tsx` · `JoinRoom.tsx` · `Lobby.tsx`
- `src/components/RoomStandings.tsx` — `RoomClock` (รหัสห้อง+เวลาถอยหลัง ในแถวไอคอน) + `RoomStandings` (แผงอันดับในแถบขวา)

**ไฟล์ที่แตะ (เล็กน้อย)**
- `store.ts` — เพิ่ม slice `room` (code / teamToken / rules / standings / endsAt) · **ห้ามแตะ logic เกมเดิม** · เพิ่มแค่ตัวส่ง heartbeat ที่อ่าน `kingCoins` ไปรายงาน
- `Home.tsx` — ปุ่ม "สร้างห้องแข่ง" / "เข้าร่วมห้อง"
- `GameBoard.landscape.tsx` — วาง `RoomStandings`
- `GameOver.tsx` — ตารางอันดับของทุกทีม
- `Settings.tsx` — ตอนอยู่ในห้อง ต้อง **ล็อก** `targetCoins`/`difficulty`/จำนวนผู้เล่น (แสดงเป็นสีจาง อ่านค่าจากห้อง)

**heartbeat**: ตั้ง interval เดียวใน `App.tsx` ยิง `sync` ทุก 3 วิ ระหว่าง `status==='running'` · ห้ามยิงต่อการเปลี่ยน state (เหรียญเปลี่ยนบ่อยกว่าที่คิดเพราะมีเหรียญปกติด้วย)

---

## 6) กติกาการแข่ง

- **จับเวลาเป็นหลัก ไม่ใช่ "ใครถึงก่อน"** — ห้องกำหนดเวลา (ค่าแนะนำ 30 นาที) หมดเวลาแล้ว **เหรียญกษัตริย์มากสุดชนะ** · ถ้ามีทีมถึงเป้าก่อน จบทันที
  - เหตุผล: `targetCoins = 7` ใช้เวลา ≈ 1.8 ชม. ซึ่งเกินคาบเรียน — ถ้าไม่จับเวลา นัดแข่งออนไลน์แล้วจะเล่นไม่จบพร้อมกัน
- **เสมอ** ตัดสินด้วย เหรียญกษัตริย์ → เหรียญปกติ → ใครถึงก่อน (`finished_at`)
- **1 แท็บเล็ต = 1 ทีม** ในเครื่องยังเล่น 1-4 คนผลัดกันเหมือนเดิม · คะแนนทีม = เหรียญรวมทุกคนในเครื่อง
  - ⚠️ **ห้องต้องล็อกจำนวนผู้เล่นต่อเครื่องให้เท่ากันทุกทีม** ไม่งั้นทีม 4 คนได้ทอยเต๋าบ่อยกว่าทีม 2 คนเท่าตัว = ได้เปรียบฟรี
- **เวลาทั้งหมดอิงนาฬิกา server** — นาฬิกาแท็บเล็ตแต่ละทีมไม่ตรงกัน (ตอนนี้ `AnswerPage` ใช้ `Date.now()` ของเครื่อง ซึ่งใช้ในห้องเรียนเดียวได้ แต่ออนไลน์ไม่ได้)

---

## 7) ความยุติธรรมของคลังคำถาม (กับดักที่ต้องกันตั้งแต่แรก)

ครูแต่ละทีมแก้การ์ดเองได้ผ่าน CMS/Excel ถ้าคลังไม่เหมือนกัน การแข่งไม่มีความหมาย

- ห้องเก็บ `contentVersion` (มี `app_config.content_version` อยู่ในสคีมาแล้ว) ไว้ตอนสร้าง
- ตอน `join` เครื่องต้องซิงก์เนื้อหาให้ตรงก่อน — **ไม่ตรง = เข้าห้องไม่ได้** พร้อมข้อความบอกให้กดซิงก์
- **เคสจริงที่จะเจอ:** เครื่องที่เปิดแบบออฟไลน์ใช้การ์ดจาก cache/seed เก่า → ต้องบล็อก ไม่ใช่ปล่อยผ่านเงียบ ๆ
- `usedQuizIds` ของแต่ละเครื่องต่างกันได้ไม่เป็นไร — ที่ต้องเท่ากันคือ "คลัง" ไม่ใช่ "ลำดับ"

---

## 8) เน็ตหลุด (ต้องไม่ทำลายจุดขายออฟไลน์-เฟิร์สต์)

| เหตุการณ์ | พฤติกรรม |
|---|---|
| `sync` ล้มเหลว | **เกมในเครื่องเดินต่อปกติ** แถบอันดับขึ้น "ขาดการเชื่อมต่อ" แล้ว retry แบบ backoff |
| กลับมาก่อนหมดเวลา | ส่งแต้มล่าสุดขึ้นไป ตามอันดับทันได้ (`ends_at` อยู่ที่ server จึงไม่เพี้ยน) |
| ไม่กลับมาเลย | `last_seen` เกิน 60 วิ = แสดงว่า "หลุด" แต่ยังติดอันดับด้วยแต้มล่าสุด |
| เจ้าภาพหลุด | ห้องยังจบเองได้เมื่อถึง `ends_at` (อย่าให้ปุ่ม "จบเกม" อยู่ที่เจ้าภาพคนเดียว) |

---

## 9) กันโกงแบบเบา (ยอมรับโมเดลความเชื่อใจ)

กติกาอยู่ในเครื่อง → แก้ `localStorage['bg7_save']` แล้วรายงานแต้มปลอมได้ · โมเดลนี้ **ยอมรับได้** เพราะเป็นห้องเรียนที่มีครูคุม (เช่นเดียวกับที่ payload ของ QR มี index เฉลยติดไปอยู่แล้ว) แต่ควรมีตัวกันหยาบ ๆ:

- `team_token` — แก้ได้เฉพาะแถวของตัวเอง
- แต้ม **เพิ่มได้อย่างเดียว** ห้ามลด
- เพิ่มเร็วผิดปกติ (เช่น > 1 เหรียญกษัตริย์/60 วิ ซึ่งเป็นไปไม่ได้จริงเพราะต้องลงช่องทองพอดี) → **ติดธงให้ครูเห็น ไม่ใช่ปฏิเสธ** (ปฏิเสธผิดพลาดจะพังกลางคาบ)

---

## 10) ข้อมูลเด็ก (PDPA)

- ชื่อทีม = **ชื่อทีม/ชั้นเรียนเท่านั้น** ห้ามชื่อจริงเด็ก · ใส่คำอธิบายใต้ช่องกรอก + จำกัดความยาว
- ชื่อผู้เล่นในเครื่อง (ที่กรอกหน้า Home) **ห้ามส่งขึ้น server** — ส่งเฉพาะชื่อทีมกับแต้ม
- ห้องหมดอายุแล้วลบทิ้ง ไม่เก็บประวัติรายคน

---

## 11) ลำดับงาน + เกณฑ์ผ่านของแต่ละขั้น

| ขั้น | งาน | ผ่านเมื่อ |
|---|---|---|
| **0** | ย้าย `bg7-api` ไป IIS+FastCGI (ภาคผนวก C) · เพิ่ม `Cache-Control: no-store` · กติกาห้ามอัปโหลดตอนแข่ง | ยิง 20 คำขอพร้อมกันแล้วไม่มีคิว |
| **1** | ตาราง + `server/room.php` (5 action) + ล้างของเก่า | ทดสอบครบทุก action ด้วย `curl.exe` **ก่อนแตะ UI** |
| **2** | `roomApi.ts` + หน้า สร้าง/เข้าร่วม/ล็อบบี้ + ล็อกกติกาใน Settings | 2 เครื่องเห็นกันในล็อบบี้ |
| **3** | heartbeat + `RoomStandings` ใน HUD | เล่นจริง 2 เครื่อง เห็นแต้มอีกฝั่งขยับใน 3 วิ |
| **4** | หมดเวลา/ถึงเป้า → จอสรุปอันดับ | ทั้ง 2 เครื่องจบพร้อมกัน อันดับตรงกัน |
| **5** | ทดสอบกับอุปกรณ์จริง 2 เครื่อง 1 คาบ | จบได้ในคาบ ไม่มีใครค้าง |

---

## 12) ยังไม่ทำใน v1 (จงใจ)

- กระดานเดียวกันออนไลน์ (ต้องย้ายกติกาขึ้น server)
- บัญชีครูแยกรายกลุ่ม / แยกคลังการ์ดต่อทีม (ต้องทำเมื่อผู้เล่นอื่นอยากแก้เนื้อหาเอง)
- แชท/เสียงระหว่างทีม (เพิ่มภาระดูแลเด็กโดยไม่จำเป็น)
- เก็บสถิติย้อนหลัง/บอร์ดคะแนนถาวร (ต้องคิดเรื่อง PDPA ให้จบก่อน)
